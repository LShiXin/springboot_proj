# Spring Boot application configuration

# MySQL datasource
spring.datasource.url=jdbc:mysql://47.95.30.65:3306/project_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=remote_user
spring.datasource.password=Lzp1204@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Redis服务器地址（改成你的公网IP或内网IP）
spring.data.redis.host=47.95.30.65
# Redis服务器端口（默认是6379）
spring.data.redis.port=6379
# Redis密码（就是你之前设置的requirepass）
spring.data.redis.password=Lzp1204@
# 可选：连接超时时间
spring.data.redis.timeout=2000ms

# JPA / Hibernate (if using JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.tool.schema.internal=DEBUG

# JWT配置
jwt.secret=mySecretKeyForJWTandmyfirstprojecttoverifyuseridentity
# JWT过期时间（单位：毫秒），这里设置为24小时
jwt.expiration=15000